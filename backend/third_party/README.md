# Third-party policy

This directory remains empty unless GE360 must vendor a minimal, license-compatible module that cannot be consumed as a normal dependency or adapter.

Current decision: no external project is vendored. archit-app is an optional/direct Python dependency; ArchLang and openPlan3D are isolated by adapters; FreeCAD MCP and AgentCAD are reference architectures only.
